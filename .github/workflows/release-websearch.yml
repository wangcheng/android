name: Release

on:
  push:
    tags:
      - "*-v*.*.*"

jobs:
  call-release-app:
    strategy:
      matrix:
        module: ["websearch"]
    uses: ./.github/workflows/release-app.yml
    if: startsWith(GITHUB_REF, matrix.module)
    with:
      module: ${{ matrix.module }}
    secrets: inherit
